{
  "name": "@enforcer-squad/ufs-init",
  "version": "1.0.7",
  "description": "ufs init module",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "start": "tsup src/index.ts --watch --platform=node --external shelljs",
    "build": "rimraf ./dist && tsup src/index.ts --platform=node --external shelljs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^11.2.0",
    "shelljs": "^0.8.5"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4"
  }
}
